# OpenIngress — copy the matching files (do not use this file alone).
#
#   cp backend/.env.example backend/.env
#   cp frontend/.env.example frontend/.env
#
# Then set LLM_API_KEY in backend/.env. Vite only reads frontend/.env.

# See backend/.env.example and frontend/.env.example for the real templates.
